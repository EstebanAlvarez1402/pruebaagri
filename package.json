{
  "name": "mi-proyecto-vue",
  "version": "1.0.0",
  "homepage": "https://CC184-Grupo-7.github.io/Frontend-AgriLink",
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "preview": "npm run build && npx http-server dist -p 8081 -o",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.2.13",
    "vue-router": "^4.0.3"
  },
  "devDependencies": {
    "@vue/cli-service": "~5.0.0",
    "gh-pages": "^6.0.0"
  }
}