# 🚀 Guía Rápida de Inicio - AgriLink

## Pasos para ejecutar el proyecto localmente:

### 1️⃣ Instalar dependencias
```bash
npm install
```

### 2️⃣ Ejecutar el proyecto
```bash
npm run serve
```

### 3️⃣ Abrir en el navegador
El proyecto se abrirá automáticamente en: `http://localhost:8080/`

---

## ✅ ¿Ves este mensaje?

```
Note that the development build is not optimized.
To create a production build, run npm run build.
```

**¡Perfecto! Todo está funcionando correctamente.** 

Este mensaje es **completamente normal** y solo te informa que estás en modo desarrollo. NO es un error.

---

## 📦 Para desplegar a GitHub Pages:

```bash
npm run deploy
```

---

## 🎯 Comandos útiles:

| Comando | ¿Qué hace? |
|---------|-----------|
| `npm run serve` | Ejecuta en modo desarrollo |
| `npm run build` | Compila para producción |
| `npm run deploy` | Sube a GitHub Pages |
| `npm run preview` | Vista previa de producción |

---

## 💡 Diferencias clave:

### Desarrollo Local (npm run serve)
- URL: `http://localhost:8080/`
- Hot reload (cambios en tiempo real)
- Más fácil para debuggear
- **El mensaje que ves es normal aquí**

### Producción (npm run build)
- URL: `https://tu-usuario.github.io/Frontend-AgriLink/`
- Código optimizado y minificado
- Listo para usuarios finales

---

## 🔄 Flujo de trabajo recomendado:

1. **Desarrolla localmente** con `npm run serve`
2. **Prueba tus cambios** en el navegador
3. **Compila** con `npm run build` cuando estés listo
4. **Despliega** con `npm run deploy` a GitHub Pages

---

## ❓ ¿Problemas?

Consulta el archivo `README.md` completo para más detalles o la sección de solución de problemas.
